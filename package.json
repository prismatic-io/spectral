{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "tsd": "turbo run tsd",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "docs": "turbo run docs"
  },
  "devDependencies": {
    "turbo": "^1.2.11"
  }
}
